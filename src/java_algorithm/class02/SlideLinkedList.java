package java_algorithm.class02;


/**
 * 将单向链表按某值划分成坐标小，中间相等，右边大的形式
 *
 * 给定一个单链表的头节点head，节点的值类型是整型，再给定一个整数pivot。
 * 实现一个调整链表的函数，将链表调整为左部分都是值小于pivot的节点，中间部分都是值等于pivot的节点
 * 右部分都是值大于pivot的节点
 * 进阶：在实现原问题功能的基础上增加如下的要求
 * 调整后所有小于pivot的节点之间的相对顺序和调整之前一样
 * 调整后所有等于pivot的节点之间的相对顺序和调整之前一样
 * 调整后所有大于pivot的节点之间的相对顺序和调整之前一样
 * 时间复杂度为O（N），额外空间复杂度为O（1）
 *
 *
 *
 */
public class SlideLinkedList {
}
